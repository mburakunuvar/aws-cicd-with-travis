language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t burakunuvar/sample-app -f Dockerfile.dev .

script:
  #   - docker run buraku/sample-app npm run test -- --coverage
  - docker run -e CI=true burakunuvar/sample-app npm run test
# deploy:
#   provider: elasticbeanstalk
#   region: "eu-west-1"
#   app: "Deploying-to-AWS-with-TravisCI"
#   env: "DeployingToAwsWithTravisci-env"
#   bucket_name: "elasticbeanstalk-eu-west-1-388521517294"
#   bucket_path: "Deploying-to-AWS-with-TravisCI"
#   on:
#     branch: master
#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key: $AWS_SECRET_KEY
