# aws-cicd-with-travis

```bash
$ git clone https://github.com/mburakunuvar/aws-cicd-with-travis.git
# # # update the code
$ git push -u origin main

# optional for local test
$ npm install
$ npm start
```
